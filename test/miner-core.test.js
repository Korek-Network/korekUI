import test from "node:test";
import assert from "node:assert/strict";
import { DEFAULT_API_URL,MINER_PROTOCOL,applyWorkerRate,createMinerAuthHeaders,formatKrk,meetsDifficulty,miningStats,normalizeApiUrl,normalizeNodeUrl,powDigest,rewardAddress,validateInnerHash,validateResources } from "../src/miner-core.js";
test("validates Planck endpoints",()=>{assert.equal(normalizeNodeUrl("http://127.0.0.1:9833/"),"http://127.0.0.1:9833");assert.equal(DEFAULT_API_URL,"https://rpc.planck.korek.network");assert.equal(normalizeApiUrl("http://127.0.0.1:8365/"),"http://127.0.0.1:8365");assert.throws(()=>normalizeNodeUrl("file:///tmp/node"),/HTTP/)});
test("derives wormhole address",()=>{const hash="ab".repeat(32);assert.equal(validateInnerHash(hash),hash);assert.equal(rewardAddress(hash),"krk1"+"ab".repeat(20))});
test("calculates exact display and statistics",()=>{assert.equal(formatKrk("115740740"),"1.1574074");const stats=miningStats(30,3472222200n,1000,61000);assert.equal(stats.blocksPerMinute,30);assert.equal(stats.totalAtomic,"3472222200")});
test("clamps resources and supports CPU-only or GPU-only mining",()=>{const hardware={threads:8,gpu:[{id:"a"},{id:"b"},{id:"software",usable:false}]},selected=validateResources({cpuThreads:99,gpuIds:["a","b","software","a"],gpuIntensity:150},hardware);assert.deepEqual(selected,{cpuThreads:8,gpuIds:["a"],gpuIntensity:100});assert.deepEqual(validateResources({cpuThreads:0,gpuIds:["a"],gpuIntensity:1},hardware),{cpuThreads:0,gpuIds:["a"],gpuIntensity:10});assert.deepEqual(validateResources({cpuThreads:1,gpuIds:[]},hardware),{cpuThreads:1,gpuIds:[],gpuIntensity:100});assert.throws(()=>validateResources({cpuThreads:0,gpuIds:[]},hardware),/at least one CPU thread or one GPU/)});
test("creates deterministic authenticated header inputs",()=>{const headers=createMinerAuthHeaders("x".repeat(32),{method:"POST",path:"/mine",body:"{}",timestamp:123,nonce:"ab".repeat(16)});assert.equal(headers["x-korek-miner-timestamp"],"123");assert.match(headers["x-korek-miner-signature"],/^[0-9a-f]{64}$/)});
test("uses the current Planck protocol",()=>assert.equal(MINER_PROTOCOL,"korek-planck-miner/3"));
test("matches the protocol v3 SHA-256 work vector",()=>{assert.equal(powDigest("00".repeat(32),0),"6db65fd59fd356f6729140571b5bcd6bb3b83492a16e1bf0a3884442fc3c8a0e");assert.equal(meetsDifficulty("000abc"+"0".repeat(58),3),true)});
test("ignores late CPU rate messages after mining stops",()=>{const oldSession={hashes:10},newSession={hashes:20},message={type:"rate",hashes:50,elapsedMs:100};assert.equal(applyWorkerRate({message,mining:false,currentSession:null,workerSession:oldSession}),null);assert.equal(oldSession.hashes,10);assert.equal(applyWorkerRate({message,mining:true,currentSession:newSession,workerSession:oldSession}),null);assert.equal(oldSession.hashes,10);assert.equal(applyWorkerRate({message,mining:true,currentSession:oldSession,workerSession:oldSession}),500);assert.equal(oldSession.hashes,60)});
